# AIDE v0.2.0 — Self-hosting Baseline

- Adds a non-mutating preflight path across HTTP/MCP and Tutti/Broker routing gates.
- Proves the first real AIDE self-development loop through Native Codex, deterministic acceptance, and Current Truth closure.
- Makes the current Codex-backed runtime/control/UI path parity-Current on Ubuntu and Windows.
- Tightens native session affinity, Windows process-tree ownership, and cross-platform path/runtime behavior.
- Improves Codex command side-effect evidence by finalizing commandExecution at item/completed and remaining fail-closed for unknown/incomplete commands.
- Refreshes the responsive management UI and overview without introducing a second policy/data plane.

Native Harnesses are not bundled. See README.md for runtime requirements and verification commands.
